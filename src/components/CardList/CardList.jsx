import { get } from "../../Helper/get"
import { useEffect } from "react"
export const CardList = () => {
    const [items, setItems] 
    useEffect(() => {
        get().then(data)
    },[])
    return (

    )
}